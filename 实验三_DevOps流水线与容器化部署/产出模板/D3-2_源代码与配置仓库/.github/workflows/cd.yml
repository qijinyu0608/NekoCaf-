name: CD
on:
  push: { branches: [main] }
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to dev
        run: echo "TODO helm upgrade --install"
      - name: Canary
        run: echo "TODO 5% -> 100%"
